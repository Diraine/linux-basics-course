
1. What is the IP address of the DNS Server used in this system?

```
Run: `cat /etc/resolv.conf` and look for the line starting with `nameserver.` The IP after `nameserver` is the DNS server used by the system.
```



